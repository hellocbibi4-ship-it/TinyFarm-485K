{
  "name": "TinyFarm Devcontainer",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "forwardPorts": [8080],
  "postCreateCommand": "echo 'Java:' && java -version && echo 'Maven:' && mvn -v || true && echo 'Installing Python and pytest...' && sudo apt-get update && sudo apt-get install -y python3-pip python3-venv && pip3 install --break-system-packages pytest && echo 'pytest:' && pytest --version || true",
  "remoteUser": "vscode"

}


